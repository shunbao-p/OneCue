# Copy this file to config.ini only when manual local configuration is needed.
# The connection helper can also create/update config.ini automatically.
[server]
port = 8787

[dots]
root =
port = 7860
