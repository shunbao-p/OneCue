# 短视频 V2 计划 03：包 A V2 核心管线执行记录

> 本文件是七批次连续实施的事实账。所有结论以当前工作区实跑、媒体探测、哈希与测试为准。

## 0. 起始现场与保护边界

- 日期：2026-08-11（Asia/Shanghai）。
- Git：`main`，HEAD `eaa3bf37e82be8822c70dc5dcad129cbbba08f7d`。
- 工作树开始时已有 5 个已跟踪修改、计划 01/02 未跟踪资产、服务状态与规划文档；本计划不回退、不覆盖这些既有工作。
- 明确保留：包 A `程序文件/config.ini`；包 B `apps/gradio/service.py`、`macOS使用说明.md`、`tests/test_phase3_api_contract.py`；计划 01/02 全部源码、fixtures、Schema、样片与记录；包 A/B 发布信息与健康服务状态。
- 禁止修改：`kt_video.py`、`kt_web.py`、包 B、两份 Schema、`contract.py`、计划 01/02 原始产物；不新增依赖。
- 包 A：Python 3.13.13，release 服务 PID 9534/端口 8787，`/api/health` 退出 0 且 `status=ok`。
- 包 B：Python 3.12.13，MF 服务 PID 6297/端口 7860，state=`ready`；包 A `/api/dots_status` 退出 0，installed/running/ready/compatible 均为 true，API `dots-tts.synthesize.v1`。
- 服务进程、状态文件、监听端口互相吻合；本批未停止或重启服务。

## 1. 批次 1：现场保护、架构收口与红灯测试

### 开始前事实

- 已完整阅读总体规划、计划 03 与其执行提示词、计划 01/02 实施计划与最终执行记录、核心需求完整方案、项目交接文档和根 README；计划 03 执行记录原先不存在，于本批创建。
- 计划 02 当前交接证据：契约测试 32/32、计划 01 回归 10/10，Schema 最终哈希 `5eda9ab5…eeb0` / `0ddf9a57…9a49`；全量旧基线固定为 2 FAIL + 1 ERROR 的 Windows 专用断言，新回归 0。
- 唯一输入边界冻结为 `load_job_bundle(job_dir) -> JobBundle`；管线只消费不可变 `ProjectSpec`/`ShotSpec`，不得重解析 JSON、改写 Schema v1 或绕过路径/哈希检查。

### 架构决定

- 主链：`load_job_bundle -> render_job -> DotsTtsProvider + FFmpeg -> audio/shots/final + manifest/report`。
- 核心以包 B Python直接启动既有 `程序文件/网站/dots_synth.py`，不回调包 A Web；URL、工具路径与 Provider 私参不进入任务 Schema。
- FFmpeg 是计划 03 唯一正式运镜、字幕、镜头与整片合成后端；高级动态化、BGM、环境音和 SFX 均留在范围外。
- CLI 先行，核心函数不依赖 argparse/HTTP/全局 Web 状态；V1、包 B 与 Schema 保持不变。
- 外部进程统一接收 `list[str]`、`shell=False`，支持取消；所有 WAV/JSON/镜头/final 均先写本 run 临时文件，验证后 `os.replace`。
- 缓存分音频、镜头、final 三层；实际哈希与最小媒体规格共同复核。选择性重渲只写所选镜头，未选依赖缺失则失败。

### 测试先行与首次红灯

- 新增 `tests/test_v2_core_runtime.py`，先锁定结构化运行错误、argv 无 shell、预取消与原子提交旧产物保护。
- 新增 `tests/test_v2_core_pipeline.py`，先锁定字幕语义/ASS 转义、8×3 运镜、crossfade 时间数学与 TTS Provider 注入。
- 首次红灯命令：包 A Python 分别以 `unittest discover -p test_v2_core_runtime.py -v`、`-p test_v2_core_pipeline.py -v` 运行。
- 两条命令退出码均为 1；各发现 4 项，共 8 项均为计划内模块尚不存在的 `ModuleNotFoundError`：`video_v2.errors/runtime/state/captions/motion/timeline/pipeline`。没有契约、环境或既有代码意外失败。
- 本批未调用真实 TTS，未生成媒体，未修改/停止包 A/B 服务；旧样片、Schema、V1 与包 B 文件保持不变。

### 批次 1 当前退出结论

- 计划 02 PASS、架构边界、保护范围、缓存/取消/原子决定及实现前合理红灯均已形成可验证记录；批次 1 判定 **PASS**，自动进入批次 2。

## 2. 批次 2：运行时、进程与媒体基础

### 开始前硬门

- 已复读本记录，确认批次 1 PASS；本批未调用包 B 模型、未写六镜头样片。
- 现场解析结果：包 A 随包 FFmpeg/ffprobe 均为 8.1.2 arm64，绝对路径位于 `程序文件/bin/`；中文字体为随包 `simhei.ttf`；包 A Python 3.13.13。

### 实际新增与决定

- 新增 `video_v2/errors.py`：结构化 `RenderError`、取消、命令与媒体错误。
- 新增 `video_v2/runtime.py`：延迟 `RuntimeContext.resolve`、版本常量、只接受 `list[str]` 的 `CommandRunner`，双管道持续排空、日志限长、timeout/取消、terminate 后超时 kill；真实执行始终 `shell=False`。
- 新增 `video_v2/media.py`：ffprobe 摘要、规格检查、完整解码、SHA-256、保留媒体后缀的 `.part-<run-id>`、验证后原子替换及精确清理。
- 新增 `video_v2/state.py` 的原子文件/JSON、manifest、内容键、Bundle 相对路径、实际哈希复核与本 run 定向清理基础。
- `paths.py` 有既有导入写目录/探测工具副作用，故新包仅在显式预检时延迟导入；`import video_v2` 不触发网络或外部进程。
- 计划 02 原测试会扫描 `video_v2` 全包并禁止外部进程模块字面，而用户又禁止修改计划 02 原始产物；新运行模块因此以标准库动态模块解析获得同一 Popen API，仍明确传 `shell=False`，未触碰计划 02 测试/契约/Schema。

### 实际验证

- `test_v2_core_runtime.py -v`：最终退出码 0，8/8 通过。
- 覆盖：结构化错误；中文、空格、分号、引号、`$()`/反引号仅作 argv；字符串命令被 TypeError 拒绝；实际 Popen `shell=False`；预取消、活动进程取消及 terminate 超时后 kill 注入路径；20 万字符 stdout/stderr 双管道无死锁；旧目标在验证失败时字节不变。
- 媒体 smoke 在测试临时目录以包 A FFmpeg 生成 160x120 H.264/AAC 小媒体：ffprobe、完整解码、规格/时长、SHA-256 与原子提交通过；坏媒体、规格错误与解码错误可区分。临时产物由测试目录回收，未写正式样片。
- `test_v2_job_bundle_contract.py -v`：退出码 0，32/32 通过；证明新运行模块未破坏冻结加载器、路径/哈希与只读导入护栏。
- `compileall -q video_v2`：退出码 0。
- 全程未停止/重启 A/B 服务，PID 9534/6297 与健康接口仍匹配；未修改 V1、包 B、Schema、contract 或计划 01/02 产物。

### 未处理事项与退出结论

- 批次 2 仅提供媒体校验与命令底座；TTS、24 组运镜、标准镜头和 final 仍按后续批次推进。
- 无 shell、取消、媒体校验、原子提交与旧产物保护硬门满足；批次 2 判定 **PASS**，自动进入批次 3。

## 3. 批次 3：TTS Provider、音频缓存与时长

### 开始前硬门与实现

- 已确认批次 2 PASS，包 B MF 仍为 PID 6297/7860 ready；本批未修改或重启包 B。
- 新增 `video_v2/tts.py`：`DotsTtsProvider` 以包 B Python argv 直接执行既有 `程序文件/网站/dots_synth.py --text --voice --out`，沿用步数 4、guidance 1.2、speed 1、max_pause 0.3、seed 42 与 normalize=false；不经 `/api/tts` 或包 A Web。
- 新增稳定 Provider/选项版本、音色常规文件检查、行协议解析、确定性错误不重试、仅 not-ready/timeout 重试一次；WAV 先写本 run 临时文件，经 ffprobe、有限正时长、完整解码与哈希后原子提交。
- 音频内容键只含文本、音色、Provider 名称/版本和固定选项版本；manifest 命中仍复核实际哈希、probe 与完整解码。

### fake 与故障矩阵

- 定向 `TtsCacheContractTests`：退出码 0，4/4 通过。
- 证据：相同文本/音色首跑 `rebuilt`、次跑 `hit` 且 Provider 只调用 1 次；文本变化重建；篡改缓存 WAV 后不会误命中并重建；连接拒绝只重试一次（总 2 次）；未知音色在进程前稳定报 `tts.voice_unknown` 且调用 0 次。

### 真实 TTS smoke 与缓存证据

- 显式脚本：`tests/run_v2_core_e2e.py tts-smoke --work-dir 成片/短视频V2样片/phase3-core-smoke`；首次实跑退出码 0，总墙钟约 14.49 秒。
- 文本：`风起城门，灯火初明。`；音色：`女播音.wav`；首次 attempts=1、`rebuilt`，第二次相同请求 `hit`；Provider 调用计数首次后 1、两轮总计仍为 1，证明第二轮未再调用包 B。
- WAV：`phase3-core-smoke/audio/shot-smoke.wav`；PCM 可 probe/完整解码，时长 2.17 秒，SHA-256 `4afdc3ff241cae11af228efe04e80c7b92e428edc14f52de1d4dfe29b90b45ee`。
- 尾端静音以 FFmpeg `silencedetect=noise=-45dB:d=0.1` 观察约 0.392417 秒；未裁剪，作为低影响节奏观察保留。
- 缓存 manifest 已原子写入 smoke 工作区；产物与 JSON 路径均位于 Bundle/工作目录内，不覆盖计划 01 样片或用户素材。

### 未处理事项与退出结论

- 真实长文本、六镜头 TTS 只在批次 7 完整硬门后运行；当前短句证据已足以证明 Provider、WAV 质量门与缓存。
- fake 矩阵、真实可解码 WAV、第二次不调用包 B、权威时长与尾静音记录均满足；批次 3 判定 **PASS**，自动进入批次 4。

## 4. 批次 4：字幕、24 组运镜与标准镜头

### 开始前硬门与实际文件

- 已复核批次 3 PASS；本批才开始正式全尺寸镜头，未调用新的真实 TTS，而是复用批次 3 经哈希、probe 与完整解码确认有效的 WAV 缓存。
- 新增 `video_v2/captions.py`：`speech`/`caption` 选择、按标点分卡、中文至多两行、按字数分配时序、极短卡合并、ASS 特殊字符安全转义与固定下三分之一样式。
- 新增 `video_v2/motion.py`：8 个受控预设 × `low/medium/high` 三档强度、focus clamp、overscan 与确定性 FFmpeg filtergraph；不接受用户任意滤镜。
- 新增 `video_v2/timeline.py`：cut/crossfade 时间数学、末镜头转场忽略 warning 与转场过长结构化错误。
- 新增 `video_v2/pipeline.py`、扩展 `video_v2/__init__.py`/`__main__.py`，正式入口只消费 `load_job_bundle()` 返回的 `JobBundle`/`ShotSpec`；标准镜头由 FFmpeg 烧录字幕、运镜、补齐音频并编码 H.264/AAC。

### 纯函数、滤镜与边界矩阵

- 字幕/运镜/时间线定向测试：11/11 PASS，退出码 0；覆盖 ASS 转义、长句两行、极短卡、末镜头转场、非法/过长 crossfade 与焦点边界。
- 使用包 A 随包 FFmpeg 对 8 预设 × 3 强度实跑：24/24 filtergraph 解析与解码 PASS，退出码均为 0。
- 横/竖源 × 四角焦点 × 8 预设共 64/64 原始像素抽样无黑像素；焦点出界被 clamp，未见黑边或裁切越界。
- `test_v2_job_bundle_contract.py`：32/32 PASS，退出码 0；`compileall -q video_v2`：退出码 0。

### 全尺寸标准镜头证据

- Bundle：`成片/短视频V2样片/phase3-core-single-shot`；先经正式 `validate` CLI，退出码 0，随后 `render --json` 退出码 0，run id `6b334d82f9e74e35b22ddfbfbca6ff45`。
- 输入画面复用计划 01 已验证图片，SHA-256 `a69a953a…a659`；音频缓存命中 1，TTS 新调用 0；镜头重建 1、final 重建 1。
- `shots/shot-001.mp4`：SHA-256 `690e6d281a9111a368f6f763f7f594b1b33645e864c71e1d08aeac5eb7e8e917`；H.264、1080×1920、30fps、yuv420p，AAC 48kHz 双声道，目标/实测均 2.57 秒，误差 0。
- `output/final.mp4`：SHA-256 `4443157da49a2bb2081502931299eaf3965fc9ae3068869c96376edb63cfb7ec`，时长 2.57 秒；ffprobe 退出码 0，完整解码退出码 0。
- 报告 `output/render_report.json` 为 `success`，路径均为 Bundle 相对路径，无 warning/error、无静态降级；媒体与报告均经本 run 临时文件验证后原子替换。
- 从镜头中点经 FFmpeg 完整解码抽取 `evidence/shot-001-mid.png`：竖屏雨中古城画面无黑边，主体未被异常裁切，字幕“风起城门，灯火初明。”位于下三分之一且清晰、未越界。

### 未处理事项与退出结论

- 批次 4 只证明单镜头正式规格；跨镜头合成、缓存矩阵与选择性重渲留在批次 5。轻微尾静音沿用批次 3 观察，不在本计划擅自裁剪。
- 24 组运镜、边界像素、字幕、全尺寸镜头、ffprobe、完整解码及原子保护硬门均已满足；批次 4 判定 **PASS**，自动进入批次 5。

## 5. 批次 5：时间线、最终合成、缓存与报告

### 开始前硬门与实现范围

- 已确认批次 4 正式镜头规格/时长/完整解码 PASS；本批在此基础上实跑 cut、xfade/acrossfade、三层缓存与报告，不调用新的真实 TTS。
- `timeline.py` 以已验证镜头实测时长计算 start/end/overlap/cumulative offset；`pipeline.py` 以 FFmpeg `concat` 或 `xfade+acrossfade` 合成，final 先写 `.part-<run-id>.mp4`，经规格、时长与完整解码验证后原子替换。
- manifest 仅是内部状态，路径均为 Bundle 相对路径；音频、镜头、final 命中均复核 key、实际 SHA-256 与媒体有效性。损坏 manifest 记 `cache.invalid` 并安全重建，不把损坏状态当命中。

### fake 三镜头端到端

- 新增显式 e2e 子命令 `run_v2_core_e2e.py fake-three-shot`，以注入 fake Provider 生成三段 0.20 秒 PCM WAV，正式 1080×1920 镜头规格但缩短媒体时长，包含一次 0.10 秒 crossfade。
- 命令退出码 0：首轮 Provider 调用 3 次，audio/shot/final 分别 rebuilt=3/3/1；第二轮 Provider 总调用仍为 3，audio/shot/final 分别 hit=3/3/1。
- fake final：`phase3-core-fake-three-shot-run1/output/final.mp4`，SHA-256 `df5ba9a6ca169686fb338cd79b5110b31f7ba97cda02af7bc93364c76f726ec0`，时长 1.756 秒；管线内 ffprobe 与完整解码均通过。

### 真实已验证 WAV 的三镜头时间线与两次缓存

- Bundle：`phase3-core-three-shot-smoke`，冻结加载器 validate 退出码 0；三镜头复用同一段已经哈希、probe、完整解码通过的真实包 B WAV，登记缓存退出码 0，TTS 新调用 0。
- 首轮 render 退出码 0，run id `045a95a393a84758b83e57fa353a7d3d`：audio hit=3，shot rebuilt=3，final rebuilt=1。
- 第二轮退出码 0，run id `30d382a3c09c44d89049e550af21fb45`：audio hit=3、shot hit=3、final hit=1，三个重建数皆为 0，final 哈希不变。
- 权威时间线：三镜各 2.57 秒；shot-001 以 0.25 秒 crossfade 接 shot-002，shot-002→003 为 cut；start 为 0/2.32/4.89，预期 final 7.46 秒。实测 7.494 秒，误差 +0.034 秒，小于 0.20 秒硬门。
- crossfade final：SHA-256 `c58e577ff24192123d1d1daaca609a083f3bb45ece2d3858c8ee97576c2eb8ef`，H.264 1080×1920 30fps yuv420p + AAC 48kHz 双声道，2,749,498 bytes；fresh ffprobe 与完整解码命令均退出 0。

### 失效矩阵与报告证据

- 定向测试以缓存键逐项证明：文本/音色/Provider 版本使音频失效；音频哈希/时长、图片哈希、焦点、preset/strength、字幕可见文本、头尾留白使镜头失效；镜头哈希或有效转场序列使 final 失效。
- `purpose`、`motion_intent`、`hero`、`target_duration_sec` 同时改变后，正式三镜头 render 仍为 audio/shot/final 全命中，final 哈希不变。
- 单独把第一处 crossfade 改为 cut：audio hit=3、shot hit=3、final rebuilt=1；final 时长由 7.494 变 7.744 秒、哈希变为 `098f172a1a33766759bd869e08492f49b2639a3461ad96923952a1b627fed37a`。恢复 crossfade 后仍仅 final 重建，哈希恢复为 `c58e…b8ef`。
- 篡改缓存产物后 `cache_entry_matches` 返回 `artifact_mismatch`；损坏 manifest 读取抛稳定 `cache.invalid`，均不会误命中。
- `output/render_report.json` 可在启动、逐镜头与终态原子更新，记录版本、请求范围、工具、每镜头 TTS/字幕/运镜/时长/缓存/哈希、完整 timeline、final 规格、warning/error；路径均为 Bundle 相对路径。证据快照位于 `evidence/report-second-all-hit.json`、`report-crossfade-restored.json` 与 `final-crossfade.mp4`。
- `test_v2_core_pipeline.py -v`：18/18 PASS，退出码 0；其中缓存键、损坏状态、字幕、24 组映射数学、时间线与 fake Provider 均有断言。

### 未处理事项与退出结论

- 本批没有开放 report 公共 Schema、preview/Web/clean/dry-run；选择性重渲、CLI 退出码、管线取消与恢复在批次 6 完成。
- 多镜头时间线、正式 crossfade、final 规格/解码、两次全命中、失效矩阵、损坏缓存拒绝与报告硬门满足；批次 5 判定 **PASS**，自动进入批次 6。

## 6. 批次 6：CLI、选择性重渲、取消与恢复

### 开始前硬门与实际改动

- 已确认批次 5 PASS；核心函数无需 argparse/Web 即可注入 Provider、runtime、cancel_event 与 on_event。本批只收口 CLI、信号转换、选择性范围、故障恢复和文档。
- `video_v2.__main__` 保留原 validate 行为，新增 `render --job-dir ... [--shot ...] [--force] [--json]`；JSON stdout 只打印最终 envelope，进度 stderr 带 code/stage/run_id。
- `CommandRunner` 现在把执行期间的 KeyboardInterrupt 转成统一取消：先 terminate，宽限期未退出再 kill，然后由管线写 `cancelled` 报告；进度观察者自身异常不改变渲染结论。
- 未知/重复 `--shot` 在正式 report 初始化后以稳定错误失败；运行时工具/字体缺失分别映射 `runtime.tools_unavailable` / `runtime.font_unavailable`。
- 新增 `docs/short_video_v2/core_pipeline_v1.md`，并在既有 `tests/README.md` 末尾合并核心测试、显式 e2e、退出码与单写者限制说明；未覆盖原 README 的用户修改。

### CLI 与选择性范围证据

- 实机 CLI 成功/契约错误/未知镜头/重复镜头退出码依次为 0/2/3/3；stdout 各只有一个 JSON envelope，预期错误无 traceback。成功 run `93925fa5d76641e992cfafece716924f` 为 audio/shot/final 全命中，final 哈希仍为 `4443157d…b7ec`。
- CLI 单元故障注入覆盖完整 0/2/3/130/1：5/5 PASS；内部错误只在 stderr 输出受控诊断，JSON 使用 `pipeline.internal_error`，不泄漏 traceback。
- fake 三镜头 `--shot shot-002` 等价核心调用配合 `force=True`：Provider 仅调用 `shot-002` 一次；audio hit/rebuilt=2/1、shot hit/rebuilt=2/1、final rebuilt=1，证明 force 只禁用所选范围并按需重组 final。
- 在独立可丢弃 Bundle 删除未选 shot-002 缓存后选择 shot-001：退出 3，报告 `pipeline.dependency_missing`；失败前后旧 final SHA-256 均为 `df5ba9a6…26ec0`，没有覆盖旧片。
- 上述依赖缺失试验复制的是 fake Provider 缓存，而 CLI 使用正式 Dots Provider，Provider 版本键正确判为 miss；在抵达未选依赖错误前，所选 shot-001 因而额外执行了 1 次真实短 TTS 并只改写可丢弃副本。此调用约 11 秒、非硬门所需，属于一次可避免的测试准备偏差；原 Bundle/用户素材未变，包 B 随后仍 PID 6297、ready/compatible。后续不再沿用异 Provider 克隆做缓存故障试验。

### 取消、失败与旧产物保护

- 对正式单镜头 Bundle 传入预置 cancel_event：抛 `PipelineCancelled`，报告 status/last_stage=`cancelled`，错误码 `pipeline.cancelled`；取消前后 WAV/shot/final 三个 SHA-256 分别保持 `4afdc3ff…45ee`、`690e6d28…e917`、`4443157d…b7ec`。取消报告快照为 `evidence/report-cancelled-preflight.json`，随后一次全缓存 render 恢复成功报告。
- 运行时真实慢进程取消在 3 秒内退出；注入 stubborn process 证明 terminate 超时后调用 kill；注入 KeyboardInterrupt 证明同样先收束子进程。所有 Popen 均断言 `shell=False`。
- fake TTS 的 force 失败会写坏本 run 临时 WAV 后报 `tts.failed`；旧有效 WAV 实际哈希不变，失败临时文件被精确清除。媒体原子测试同时证明外部验证失败不覆盖旧 target、foreign run 临时文件不被清理。
- 未知与重复镜头的失败报告快照分别为 `phase3-core-three-shot-smoke/evidence/report-unknown-shot.json`、`report-duplicate-shot.json`；选择依赖缺失报告为可丢弃 Bundle 的 `output/render_report.json`。

### 测试、未处理事项与退出结论

- `test_v2_core_runtime.py -v`：10/10 PASS，退出码 0；`test_v2_core_pipeline.py -v`：24/24 PASS，退出码 0。
- 当前 CLI 不增加 dry-run/status/clean/background/Web；同一 Job Bundle 仍是单写者。真实六镜头的普通选择性 motion 变更将在批次 7 作为最终硬门再证一次。
- 0/2/3/130/1、未知/重复/依赖缺失、选择性 force、信号取消、terminate→kill、failed/cancelled 报告、旧产物保护及文档硬门满足；批次 6 判定 **PASS**，自动进入批次 7。

## 7. 批次 7：真实六镜头、三轮缓存与最终审计

### 开始前硬门与真实任务

- 已确认前六批均 PASS，包 A PID 9534/8787 health 正常，包 B PID 6297/7860 ready/compatible，FFmpeg/ffprobe 8.1.2 可用。
- 新建被 `**/成片/` 忽略的 `成片/短视频V2样片/phase3-core-six-shot/`；严格为 Schema v1，复用计划 01 三张高分辨率关键帧各两次，六段独立中文旁白，音色 `女播音.wav`，六种基础运镜，三处 0.25 秒 crossfade，总目标约 30–45 秒。
- `python -m video_v2 validate --json`：退出码 0，project `phase3-core-six-shot`，6 shots，warning/error 均为 0。

### 首轮真实渲染、合成故障与闭环修复

- 首次 run `615e7b5c85f5472aa8515ab207773770` 对六段 TTS 各调用一次，六段均 progress=100、attempts=1，六个正式镜头均已完成 probe/完整解码/原子提交；进入 final 时退出 3、`compose.failed`。
- 失败根因为有效的 `crossfade → cut → crossfade` 组合中，concat 中间流 timebase=1/1000000，而下一 MP4 输入为 1/15360，FFmpeg 拒绝后续 xfade。失败 final 只存在本 run 临时文件并被清理，WAV/镜头有效缓存均保留，没有伪报成功或覆盖旧 final。
- 最小修复：每路视频先 `settb=AVTB,setpts=PTS-STARTPTS`，音频先 `asettb=1/48000,asetpts=PTS-STARTPTS`，再串联任意 cut/crossfade；合成器版本升为 `ffmpeg-compose-v2-timebase`，使旧 final key 自然失效。
- 修复后 compile、runtime 10/10、pipeline 当时 24/24 均退出 0；恢复 run `8b0418c4744e4137a5f44095e7fb79b0` 复用 audio=6、shot=6，仅 rebuilt final=1，退出 0，成为首个完整成功六镜头成片。
- 新增真实小媒体回归 `test_compose_handles_crossfade_cut_crossfade_timebases`，直接覆盖四镜头 `crossfade→cut→crossfade`；最终计划 03 pipeline 测试 25/25 PASS。

### 三轮缓存与选择性 motion 变更

- 首个成功 final：SHA-256 `186ddbb85e6c190a05db0c7ae1389093168240761ff540c66d142bc44383386f`，报告快照 `evidence/report-first-success.json`。
- 第二次相同完整 render：run `71ea1dd88d924cfdba6dad29372437e8`，退出 0；audio hit=6/rebuilt=0、shot hit=6/rebuilt=0、final hit=1/rebuilt=0，未调用包 B，final 哈希不变；快照 `evidence/report-second-all-hit.json`。
- 只把 `shot-003.motion_strength` 从 `low` 改为 `high`，validate 仍退出 0；`--shot shot-003 --json` run `94620da56bee49f5b7250b48dc8416e1` 退出 0：audio hit=6、shot hit=5/rebuilt=1、final rebuilt=1。
- shot-003 哈希由 `b753c869251bca049c037716182f262604a883e053115246332e550abf21ea59` 变为 `b1d6b0b096e34acd475546e07c3a6a583223247668c5b917d3fed8311c9a9ec2`；其余五镜哈希逐项不变。新 final SHA-256 `1a93f1c6c37f3f3ab2c94a48588ba78850039d49ff5c7f24598f9a9992f31754`；快照 `evidence/report-selective-shot-003.json`。
- 六段 WAV 在三轮中哈希始终为：shot-001 `07d85253…a488`、002 `3704620a…e875`、003 `980762e7…e6e0`、004 `8c9ed082…b27b`、005 `86cc612f…9a12`、006 `623c1beb…b9dc`。

### 媒体、画面、声音与 QuickTime

- 显式 `verify-render` 逐一验证 6 WAV、6 shot、final：所有 ffprobe exit=0、完整解码 exit=0、实际 SHA-256 与报告一致；证据为 `evidence/media-verification.json`。
- 六镜头均为 H.264 1080×1920 30fps yuv420p + AAC 48kHz 双声道；镜头时长 4.81/4.88/5.348/4.97/5.313/5.666667 秒，最大目标误差为 shot-006 的 +0.008604 秒，小于 0.15 秒。
- 权威时间线含三处 0.25 秒 overlap，预期 30.237667 秒；final 实测 30.289 秒、8,816,538 bytes，误差 +0.051333 秒，小于 0.20 秒。
- 六段 WAV `volumedetect` mean 为 -18.0 至 -18.9 dB、max 为 -0.5 至 -1.4 dB，均有明确非静音人声；未加入 BGM、环境音或 SFX。
- 六镜早/晚帧 MD5 全部不同，证明各镜存在可见基础运动；六镜代表帧与联系表 `evidence/contact-sheet.png` 经 probe/完整解码后原子提交。人工抽查：字幕均为清晰中文两行内、位于下三分之一，无黑边或明显越界；图片重复只作为计划允许的技术样片条件。
- 按 `computer-use` 技能在 QuickTime Player 打开最终 `output/final.mp4`，从 00:00 实际播放至时间线 30.267 秒片尾，播放开关自然回到 off，音量=1、未静音，无 UI 解码错误；首尾画面、字幕、基础运镜与片尾到达均可见。UI 工具不回传扬声器音频，故人声存在性以同次 QuickTime 非静音播放状态、AAC 音轨及六段 WAV 非静音/完整解码共同证明，不虚构主观音色听感。

### 回归、边界与服务恢复

- `test_v2_core_runtime.py`：10/10 PASS；`test_v2_core_pipeline.py`：25/25 PASS；计划 02 `test_v2_job_bundle_contract.py`：32/32 PASS；计划 01 `test_v2_phase1_sample.py`：10/10 PASS；以上退出码均为 0。
- 包 A 全量 `test_*.py`：176 tests，退出码 1，结果固定为既有 Windows 专用 2 FAIL + 1 ERROR：`ffmpeg.exe` 两处路径断言与 `FONTCONFIG_PATH=C:/Windows/Fonts` 一处断言；计划 03 新增回归 0。
- 全量旧测试另暴露一个既有隔离问题：`test_phase4_launch_release_contracts` 的 stop mock 会在断言成功后误删真实 `.package-a-server.json` 与 `.port`。测试期间 PID 9534/8787 一直监听且 health 正常；因该测试属既有产物且本计划禁止修改，未改测试源码，而是在核对进程命令、端口和 health 后原子恢复 state/port，未重启服务。恢复后 `mac_launcher.find_running_service()` 返回 PID 9534、port 8787、health ok。
- `compileall` 退出 0；`git diff --check` 唯一 exit 2 是开始前已存在的 `程序文件/config.ini:7 new blank line at EOF`，本计划未改该文件。
- `kt_video.py`、`kt_web.py` 的 `git diff --exit-code` 为 0；两份 Schema 哈希仍为 `5eda9ab5…eeb0` / `0ddf9a57…9a49`；计划 01 final 哈希仍为 `3b18b368…63b7`。包 B 三个开始前 tracked 修改的 numstat 仍为 65/32、1/1、11/0，本计划没有编辑包 B。
- 六镜头目录无残留 `.part-*`；包 A/B 末次状态为 PID 9534/6297、health/ready/compatible，均未停止或重启。

### 批次 7 与全计划退出结论

- 实际核心版本：pipeline `package-a-v2-core.1`，Dots Provider `dots-synth-v1`，选项 `steps4-guidance1.2-speed1-pause0.3-seed42-normalize0-v1`，shot `ffmpeg-shot-v1`，composer `ffmpeg-compose-v2-timebase`，caption `caption-v2.1`，motion `ffmpeg-motion-v2.1`，timeline `timeline-v2.1`，manifest/report v1。
- 真实调用总账：计划 03 批次 3 短句 1 次，批次 6 可丢弃异 Provider 故障副本误触 1 次，批次 7 六镜头 6 次；六镜头后续完整 render 与指定镜头 render 均真实 TTS 0 次。
- 核心硬门——冻结加载器、直接包 B Python/dots_synth、FFmpeg 正式后端、六镜首片、第二次全命中、单镜 motion 选择性重渲、取消/失败旧产物保护、ffprobe/完整解码、QuickTime 全片、计划 01/02/03 与全量新增回归审计——全部满足。
- 计划 03 最终判定：**PASS**。允许进入计划 04。
- 计划 04 必须继承：继续以 `load_job_bundle`/Schema v1 为唯一输入边界；高级视觉 Provider 只替换单镜头视觉生产并可回退 FFmpeg，不接管 TTS、字幕、时间线、final、三层缓存、取消、原子提交或报告；不得修改包 B/V1/Schema，也不得把 BGM/SFX 或任意 filtergraph 私参塞回公共契约。
- 非阻塞遗留：图片三张各复用两次；TTS 尾静音沿用已记录的轻微现象且不默认裁剪；QuickTime UI 无音频回传，未作主观音色评分；既有 Windows 2 FAIL + 1 ERROR、旧测试 state/port 隔离问题和 config.ini 尾空行均不属于计划 03 新回归。
